import { StatsigUser } from '../StatsigUser';
import { EvaluationReason } from './EvaluationReason';
export default abstract class BootstrapValidator {
    static getEvaluationReasonForBootstrap(user: StatsigUser | null, values: Record<string, unknown>, stableID: string | number | null): EvaluationReason;
    private static validate;
    private static copyObject;
}
