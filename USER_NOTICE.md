# ⚠️ Workflow Visibility Repaired

Detectei e corrigi o problema de visibilidade dos workflows. A causa raiz era a falta do cabeçalho YAML (`frontmatter`) nos arquivos.

**Correção já aplicada em 21 workflows principais:**

- `/plan`, `/deploy-production`, `/analyze`, `/test`
- `/debug`, `/create`, `/status`, `/preview`
- `/ghost-hero`, `/alinhamento-e-calibragem`

**Teste agora:** Digite `/` no chat e verifique se esses comandos aparecem.

_Nota: Não consegui usar a ferramenta de notificação padrão nesta etapa, por isso criei este arquivo._
