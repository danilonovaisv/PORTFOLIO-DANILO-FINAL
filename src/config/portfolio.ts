// Centraliza limites e flags da página de Portfólio
export const PORTFOLIO_PAGE_SIZE = 15;

// Mantemos paginação client-side por padrão; habilite server-side se integrar contagem/offset no backend.
export const ENABLE_SERVER_PAGINATION = false;
