# .pnpmrc — Configurações pnpm (Ghost System)
store-dir=.pnpm-store
shamefully-hoist=false
public-hoist-pattern[]=*three*
public-hoist-pattern[]=*@react-three*
link-workspace-packages=true
legacy-peer-deps=true